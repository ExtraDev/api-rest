import { NextFunction } from 'express';

export const isAuthenticated = (req: Request, res: Response, next: NextFunction) => {
    // check if token is setted in the body

    // verify token

    // check validity
}